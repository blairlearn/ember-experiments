   {{#if folderlist}}
    <ul>
        {{#each folder in folderlist}}
        <li>{{individual-node node=folder}}
        {{#if folder.subtree}}
            {{nested-folder folderlist=folder.subtree}}
        {{/if}}
        </li>
        {{/each}}
    </ul>
    {{/if}}
